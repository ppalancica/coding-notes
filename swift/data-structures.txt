--- Stack of Int items

class Stack {
    var items: [Int] = []
    
    func push(n: Int) {
        items.append(n)
    }
    
    func pop() -> Int? {
        return items.popLast()
    }

    func peek() -> Int? {
        return items.last
    } 
}